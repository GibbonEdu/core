apiVersion: apps/v1
kind: Deployment
metadata:
  name: gibbon-app
  namespace: demo-app-deployment
  labels:
    app: gibbon
spec:
  replicas: 2
  selector:
    matchLabels:
      app: gibbon
  template:
    metadata:
      labels:
        app: gibbon
    spec:
      nodeSelector:
          role: app-demo-1
      containers:
        - name: gibbon
          image: ntony3419/gibbon:v29-pdofix
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          volumeMounts:
            - name: gibbon-uploads
              mountPath: /var/www/html/uploads
          env:
            - name: DB_HOST
              value: gibbon-mysql.demo-app-deployment.svc.cluster.local
            - name: DB_NAME
              value: gibbon
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: gibbon-db-secret
                  key: username
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: gibbon-db-secret
                  key: password
      volumes:
        - name: gibbon-uploads
#          emptyDir: {}
          persistentVolumeClaim:
            claimName: gibbon-uploads-pvc
