# `travis_ci` environment config goes here
modules:
    config:
        Db:
            dsn: "mysql:host=127.0.0.1;dbname=gibbon_test"
            user: "root"
            password: ""
            dump: '_data/dump.sql'
            populate: true
            cleanup: false
            reconnect: true
