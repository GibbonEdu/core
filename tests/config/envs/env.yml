# `env` environment config goes here
modules:
    enabled:
        - PhpBrowser:
            url: "https://127.0.0.1:8888/core"
    config:
        Db:
            dsn: "mysql:host=localhost;dbname=gibbontest"
            user: "root"
            password: "root"
            dump: 'config/data/dump.sql'
            populate: true
            cleanup: false
            reconnect: true
